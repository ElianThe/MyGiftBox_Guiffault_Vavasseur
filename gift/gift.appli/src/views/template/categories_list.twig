{% extends "_layout/main.twig" %}

{% block content %}

    <body>
    <h1>Liste des catégories</h1>
    <ul>
        {% for categorie in categories %}
            <li><a href="{{ url_for('categorie', { 'id': categorie.id}) }}">{{ categorie.id }}
                    - {{ categorie.libelle }}</a></li>
        {% endfor %}
    </ul>
    </body>
    </html>

{% endblock %}